server:
  port: 9000
spring:
  application:
    name: safe-server

eureka:
  client:
    service-url:
       defaultZone: http://localhost:9000/eureka/
    fetch-registry: false
    register-with-eureka: false

logging:
  file: ./log/safe-server.log